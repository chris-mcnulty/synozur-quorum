# Icons

Drop a 192x192 `color.png` and a 32x32 transparent `outline.png` into
this folder before sideloading. Microsoft 365 Agents Toolkit will reject
the package without them.

The icons are referenced from `manifest.json`.
